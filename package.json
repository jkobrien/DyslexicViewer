{
  "name": "dyslexicviewer",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "check-types": "turbo run check-types",
    "check": "biome check .",
    "lint": "biome lint .",
    "format": "biome format ."
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "ultracite": "latest",
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.7.3"
  }
}
